# RADICL Application Environment Variables
# Copy this file to .env and update values as needed

# ============================================
# PostgreSQL Database Configuration
# ============================================
POSTGRES_DB=radicl_db
POSTGRES_USER=radicl_user
POSTGRES_PASSWORD=radicl_password
POSTGRES_PORT=5432

# ============================================
# Database Connection for Node.js
# ============================================
DATABASE_URL=postgresql://radicl_user:radicl_password@localhost:5432/radicl_db

# ============================================
# Database Connection for Go
# ============================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=radicl_db
DB_USER=radicl_user
DB_PASSWORD=radicl_password
DB_SSLMODE=disable

# ============================================
# Service Ports (Optional - defaults shown)
# ============================================
# NODE_SERVICE_PORT=3001
# GO_SERVICE_PORT=3002
# FRONTEND_PORT=5173

# ============================================
# IMPORTANT SECURITY NOTES
# ============================================
# 1. These are DEFAULT development credentials
# 2. NEVER use these values in production
# 3. Generate strong, unique passwords for production
# 4. Never commit .env files to version control
# 5. Enable SSL/TLS for production databases
# 6. Restrict database access with firewalls

